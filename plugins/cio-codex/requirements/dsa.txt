# ─────────────────────────────────────────────────────────────────────────────
# Pinned dependencies for the OPTIONAL daily-stock-analysis (DSA) skill venv.
#
# Installed by `bootstrap.sh` (the DSA step) into  ~/.hermes/profiles/<id>/.dsa-venv
# ONLY if you opt in. The core CIO + `port` valuation engine is pure stdlib and
# needs NONE of this — these power the deeper `scan TICKER` / `ta TICKER` paths.
#
#   python3 -m venv .dsa-venv
#   .dsa-venv/bin/pip install -r requirements-dsa.txt
#   # then point DSA_VENV_PYTHON in your .env at  .dsa-venv/bin/python
#
# Versions are pinned for reproducibility; bump deliberately, not by accident.
# ─────────────────────────────────────────────────────────────────────────────
yfinance==1.3.0
litellm==1.82.6
openai==2.33.0
pandas==3.0.2
numpy==2.4.4
tiktoken==0.12.0
beautifulsoup4==4.14.3
curl_cffi==0.15.0
requests==2.33.1
pydantic==2.13.3
